# from flask_mysqldb import MySQL

# mysql = MySQL(app)

# cur = mysql.connection.cursor()

# cur.execute('CREATE TABLE users(id INT(11) AUTO_INCREMENT PRIMARY KEY), name VARCHAR(100), email VARCHAR(100), username VARCHAR(30), password VARCHAR(100), register_date DEFAULT CURRENT_TIMESTAMP);')

# cur.execute('CREATE TABLE articles (id INT(11) AUTO INCREMENT PRIMARY KEY, title VARCHAR(255), author VARCHAR(100), body TEXT, create_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP);')

# mysql.connection.commit()

# cur.close()
